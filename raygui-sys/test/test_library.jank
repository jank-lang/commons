(ns test-library
  (:require [clojure.test :as t])
  (:include "raylib.h"
            "raygui.h"))

(t/deftest library-symbol
  (cpp/GuiSetState 2)
  (t/is (= 2 (int (+ 0.0 (cpp/GuiGetState)))))
  (cpp/GuiSetState 0)
  (t/is (= 0 (int (+ 0.0 (cpp/GuiGetState))))))
